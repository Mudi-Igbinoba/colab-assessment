import React from 'react';
import Cards from '../components/Projects/Cards';

const Projects = () => {
    return (
        <main>
            <Cards />
        </main>
    );
};

export default Projects;
